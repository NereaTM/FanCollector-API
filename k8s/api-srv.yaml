apiVersion: v1
kind: Service
metadata:
  name: fancollector-api
spec:
  type: LoadBalancer
  selector:
    app: fancollector
    tier: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080